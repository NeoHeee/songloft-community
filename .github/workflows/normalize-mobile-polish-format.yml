name: Normalize mobile polish formatting

on:
  pull_request:
    branches:
      - main
    types:
      - edited

permissions:
  contents: write

jobs:
  format:
    if: github.event.pull_request.head.ref == 'mobile-polish-round-1'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: mobile-polish-round-1
          fetch-depth: 0
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.41.5'
          channel: stable
          cache: true
      - name: Format changed Dart files
        shell: bash
        run: |
          git fetch origin main
          mapfile -t dart_files < <(
            git diff --name-only --diff-filter=ACMRT origin/main...HEAD -- \
              | grep -E '^(lib|test)/.*\.dart$' \
              || true
          )
          printf 'Formatting %s\n' "${dart_files[@]}"
          if [[ ${#dart_files[@]} -gt 0 ]]; then
            dart format "${dart_files[@]}"
          fi
      - name: Validate
        run: |
          flutter pub get
          flutter analyze
          flutter test test/core/theme
          flutter test test/features/player/domain/lyric_parser_test.dart
      - name: Commit formatting
        shell: bash
        run: |
          if git diff --quiet; then
            echo "Changed Dart files already formatted"
            exit 0
          fi
          git config user.name "Songloft UI Bot"
          git config user.email "actions@users.noreply.github.com"
          git add lib test
          git commit -m "style: normalize mobile polish Dart formatting"
          git push origin HEAD:mobile-polish-round-1
